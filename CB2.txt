# ====== Py coding tricks 6/5/26 =============

None     # special object
""       # empty string
	py may reject None but accept "" which deletes whatever was there automatically

How to use None

How else is used to "back out" of action by cancelling action and doing nothing. Just feels like back out to user. Then, print some like: no changes made to tell user then "back out"  

Wrapping one def and setting aside to leave an extra tap in for future use. def fn_b --> return fn_a ...gives an extra input method for the same function object a.  


CURRENT CODE:

#***** JS_vs_Py_retrieval_1 *****

Compare JS vs Py retrieval means:

const j = prop.attr;      p = attr.mthd
j();                      p()

Enter new code (END to finish):

python -m py_compile quiz.py
python -m py_compile cards.py